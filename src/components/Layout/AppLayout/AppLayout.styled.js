import styled from 'styled-components';
import AppLayout from './AppLayout.component';

export default styled(AppLayout).attrs({})`
  height: 100%;
  margin: 0;
  background-color: transparent;
  border: none;
  outline: none;
  -webkit-font-smoothing: antialiased;
`;
